[pytest]
# Pytest configuration for CMMS tests
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    -p no:langsmith
    -p no:cacheprovider

